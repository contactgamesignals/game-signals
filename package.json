{
  "name": "gamesignal",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "node scripts/check-no-long-dash.mjs && npm run lint && npm run typecheck && next build",
    "start": "next start",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit && node --experimental-strip-types scripts/check-stripe-event-parsers.ts && node --experimental-strip-types scripts/check-ksef-crypto.ts && node --experimental-strip-types scripts/check-ksef-request-builders.ts && node --experimental-strip-types scripts/check-edge-function-drafts.ts && node --experimental-strip-types scripts/check-billing-migrations.ts && node --experimental-strip-types scripts/check-tax-access-gate.ts && node --experimental-strip-types scripts/check-seller-document-queue.ts"
  },
  "dependencies": {
    "@supabase/ssr": "^0.7.0",
    "@supabase/supabase-js": "^2.55.0",
    "next": "^16.0.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "eslint": "^9.0.0",
    "eslint-config-next": "^16.0.0",
    "typescript": "^5.8.0"
  }
}
